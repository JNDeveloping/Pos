import { useEffect, useState } from 'react';
import { Printer, Search } from 'lucide-react';
import { api } from '../lib/api';
import { branchContext } from '../lib/branch-context';
type Product={id:string;internalCode:string;name:string;barcodes:{barcode:string}[];branchConfigs:{branch:{id:string};salePrice:string}[]};
type Page={data:Product[]};
export function Labels(){const branchId=branchContext.get();const [products,setProducts]=useState<Product[]>([]),[selected,setSelected]=useState<string[]>([]),[search,setSearch]=useState(''),[template,setTemplate]=useState('SHELF_SMALL');const load=()=>api<Page>(`/products?branchId=${branchId}&enabled=true&search=${encodeURIComponent(search)}&limit=50`).then(x=>setProducts(x.data));useEffect(()=>{if(branchId)void load()},[branchId]);const chosen=products.filter(x=>selected.includes(x.id));async function print(){if(!branchId)return;await api('/labels/generated',{method:'POST',body:JSON.stringify({branchId,productIds:selected,template})});window.print()}return <><div className="no-print"><h1 className="text-3xl font-bold">Etiquetas y carteles</h1><p className="mt-2 text-slate-500">Seleccioná hasta 50 productos y utilizá la impresión del navegador para papel o PDF.</p><div className="card mt-6 grid gap-3 p-4 md:grid-cols-[1fr_220px_auto]"><label className="flex gap-2"><Search className="self-center"/><input className="flex-1" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar producto"/><button className="btn-secondary" onClick={()=>void load()}>Buscar</button></label><select value={template} onChange={e=>setTemplate(e.target.value)}><option value="SHELF_SMALL">Góndola pequeña</option><option value="SHELF_LARGE">Góndola grande</option><option value="A5_OFFER">A5 oferta</option><option value="A4_OFFER">A4 oferta</option></select><button className="btn" disabled={!selected.length} onClick={()=>void print()}><Printer size={18}/>Imprimir {selected.length}</button></div><div className="card mt-4 divide-y">{products.map(p=><label className="flex min-h-14 items-center gap-3 p-4" key={p.id}><input type="checkbox" checked={selected.includes(p.id)} disabled={!selected.includes(p.id)&&selected.length>=50} onChange={e=>setSelected(e.target.checked?[...selected,p.id]:selected.filter(x=>x!==p.id))}/><span className="font-mono text-sm">{p.internalCode}</span><b className="flex-1">{p.name}</b><span>$ {Number(p.branchConfigs.find(x=>x.branch.id===branchId)?.salePrice??0).toLocaleString('es-AR')}</span></label>)}</div></div><section className={`print-grid label-template-${template.toLowerCase()}`}>{chosen.map(p=><article className="print-label" key={p.id}>{template.includes('OFFER')&&<strong className="offer">OFERTA</strong>}<h2>{p.name}</h2><b className="price">$ {Number(p.branchConfigs.find(x=>x.branch.id===branchId)?.salePrice??0).toLocaleString('es-AR')}</b><p>{p.barcodes[0]?.barcode}</p><small>{p.internalCode} · {new Date().toLocaleDateString('es-AR')}</small></article>)}</section></>}
